osbyte_with_y0:.proc
					
		LDY	#0
                .if be_version||tbe_version
                jmp OSBYTE
                .else
		JSR	OSBYTE
                .endif
                .if !tbe_version
                RTS
                .endif
.pend
; End of function osbyte_with_y0



